import "../styles/globals.css";
			export const metadata = {
				title: "Tela de Login",
			};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
		}
